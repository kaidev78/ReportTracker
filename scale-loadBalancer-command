# Open 3 angular application and nginx will use round robin method to distribute requests
# with this command, we can easily scale up any services in one line of command.
docker-compose up --scale angular=3
